// Write a program to input an integer from the user and display the entered value.
#include <stdio.h>
int main(){
    int num;
    printf("Enter any number: ");
    scanf("%d", &num);
    printf("Your number is %d.", num);
    return 0;
}
